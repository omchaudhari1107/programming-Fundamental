# ✅ Coding Practice Questions (Easy → Hard)

---

## 🟢 Easy Level
1. **Remove duplicate numbers from a list**
2. **Find the maximum and minimum number in a list**
3. **Count how many times each element appears in a list**
4. **Reverse a string without using built-in reverse functions**
5. **Check if a string is a palindrome**
6. **Sum all even numbers in a list**
7. **Find the second largest number in a list**
8. **Swap two variables without using a third variable**
9. **Check if a number is prime or not**
10. **Merge two lists into one without duplicates**

---

## 🟡 Medium Level
1. **Find all pairs in a list whose sum is equal to a given number**
2. **Sort a list without using built-in sort methods**
3. **Rotate an array left or right by N positions**
4. **Find the longest word in a sentence**
5. **Find common elements between two lists**
6. **Count vowels and consonants in a string**
7. **Remove all special characters from a string**
8. **Find missing numbers in a list of consecutive integers**
9. **Group anagrams from a list of strings**
10. **Check if two strings are anagrams**

---

## 🔴 Hard Level
1. **Implement a simple version of binary search**
2. **Find the longest substring without repeating characters**
3. **Implement your own stack and queue**
4. **Find the first non-repeating character in a string**
5. **Find majority element in a list (appears more than n/2 times)**
6. **Check if a list has any cycle (linked list cycle detection)**
7. **Generate all permutations of a given string**
8. **Implement merge sort**
9. **Find the maximum subarray sum (Kadane’s Algorithm)**
10. **Solve the Two Sum problem without using hashmaps**

